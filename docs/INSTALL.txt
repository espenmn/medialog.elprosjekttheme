medialog.elprosjekttheme Installation
------------------------------------

* Add ``medialog.elprosjekttheme`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        medialog.elprosjekttheme
       

* Re-run buildout, e.g. with:

    $ ./bin/buildout
   
   
   